# 🌐 HTML Learning & Projects Repository

This repository contains **HTML concepts, examples, and mini-projects** created to strengthen frontend development fundamentals.  
It is useful for **beginners, interview preparation, and revision**.

---

## 📌 About HTML

**HTML (HyperText Markup Language)** is the standard markup language used to create and structure web pages.  
It defines the **content and layout** of a webpage using elements like headings, paragraphs, links, images, forms, etc.

---

## 🧠 Topics Covered

- Basic HTML Structure
- HTML Elements & Attributes
- Headings, Paragraphs & Formatting
- Links & Images
- Lists (Ordered & Unordered)
- Tables
- Forms & Input Types
- Semantic HTML
- Media Elements (Audio & Video)
- HTML5 Features

---

## 📂 Folder Structure


---

### 📁 basics/
- `index.html` → Basic HTML page
- `structure.html` → HTML document structure
- `tags.html` → Common HTML tags

### 📁 text-formatting/
- `headings.html` → `<h1>` to `<h6>` usage
- `paragraphs.html` → Paragraphs and text formatting tags

### 📁 lists/
- `ordered-list.html` → Ordered lists (`<ol>`)
- `unordered-list.html` → Unordered lists (`<ul>`)

### 📁 tables/
- `table-example.html` → Tables using `<table>`, `<tr>`, `<th>`, `<td>`

### 📁 forms/
- `login-form.html` → Login form example
- `registration-form.html` → Registration form with inputs

### 📁 semantic-html/
- `semantic-layout.html` → Semantic elements like `<header>`, `<nav>`, `<main>`, `<footer>`

### 📁 media/
- `audio.html` → Audio element usage
- `video.html` → Video element usage

---

## ▶️ How to Run the Files

1. Clone the repository
   ```bash
   git clone https://github.com/your-username/your-repo-name.git

   ## ▶️ How to Run the HTML Files

2. Open the project folder in **Visual Studio Code**
3. Open any `.html` file
4. Right-click on the file
5. Select **Open with Browser**

✔️ No setup required.

---

## 🛠 Tools Used

- HTML5
- Visual Studio Code
- Live Server Extension (optional)
- Web Browser (Chrome / Edge / Firefox)

---

## 🎯 Learning Objectives

- Understand HTML structure
- Learn core HTML tags
- Build semantic and accessible web pages
- Practice forms, tables, lists, and media elements
- Strengthen frontend basics



